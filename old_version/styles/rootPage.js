import { StyleSheet } from 'react-native';

const stylesRootpage = StyleSheet.create({
  rootPage: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'green'
  }
});

export default stylesRootpage;
